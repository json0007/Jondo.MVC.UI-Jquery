using System;
using System.Collections.Generic;
using System.Text;

namespace Jondo.Enums
{
    public enum AnimationType
    {
        Slide = 0,
        Fade = 1
    }

    public enum AnimationDirection
    {
        In = 0,
        Out = 1
    }
}
